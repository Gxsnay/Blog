{
  "name": "mobile",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "premobile": "node buildConfig.js",
    "prebuild": "node buildConfig.js",
    "mobile": "vitepress dev . --open --port 3001",
    "build": "vitepress build .",
    "serve": "vitepress serve ."
  },
  "devDependencies": {
    "stylus": "^0.55.0",
    "stylus-loader": "^6.1.0",
    "vitepress": "^0.19.1"
  }
}
